rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Sehr permissive Regeln für Debugging - alle Operationen für authentifizierte Benutzer erlauben
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}